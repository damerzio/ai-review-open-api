type: object
additionalProperties: false
description: One page of catalog entries.
properties:
  page:
    $ref: ../../../shared/components/schemas/Page.yaml
  items:
    type: array
    description: Products in this page, newest first.
    maxItems: 100
    items:
      $ref: ./Product.yaml
required:
  - page
  - items
